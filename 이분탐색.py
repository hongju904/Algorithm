right = max(lis)
left = min(lis)

while left <= right:
  mid = (left+right) // 2
  count = 0
  
  for l in lis:
    # 구해야할 코드
  
  if count >= n:
    result = mid
    right = mid - 1
  else:
    lefg = mid + 1
